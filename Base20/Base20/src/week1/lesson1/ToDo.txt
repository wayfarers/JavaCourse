+ Task1. ¬вести с консоли строку с символами и пробелами. ѕосчитать количество слов в предлождении.

+ Task2. Ќаписать метод, принимающий в качестве параметра строку. 
¬озвращающий перевернутую строку.

+ Task3. Ќаписать метод, принимающий в качестве параметра строку. 
¬озвращающий перевернутую строку, не использующий дополнительной строки.

+ Task4. Ќаписать программу состо€щую из методов:
 - метод, считывает с консоли строку и возвращает ее
 - метод, считывает с консоли символ и возращает его
 - метод, принимает строку и символ, делает поиск символа в строке, 
 возвращает индекс последнего вхождени€ символа в строке
 - главный метод, только вызовы методов.

+ Task5. Ќаписать метод boolean myEquals, который принимает две строки.(не использу€ equals)

+ Task6. Ќаписать метод int MyIndexOf, который принимает две строки.(не использу€ indexOf)